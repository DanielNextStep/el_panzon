name: taco_shop
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Firebase core dependency
  firebase_core: ^2.24.2
  # Firestore for online database storage
  cloud_firestore: ^4.13.6
  # Helper for date formatting (Required for OpenOrdersScreen)
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/Logo_Panzon_SF.png