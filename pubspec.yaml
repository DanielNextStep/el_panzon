name: ordenes_elpanzon
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^2.24.2
  cloud_firestore: ^4.13.6
  intl: ^0.18.1

  # Pinning to 4.1.7 specifically to avoid 4.2.0+ dependency issues
  image: 4.1.7
  package_info_plus: ^9.0.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/Logo_Panzon_SF.png
    - assets/images/Logo_Panzon_BN.png
    - assets/images/Panzon_BN_visita.png